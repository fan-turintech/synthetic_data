[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    distributions: Tests for distribution classes
    generator: Tests for the data generator
    integration: Integration tests for the full workflow
addopts = -v
